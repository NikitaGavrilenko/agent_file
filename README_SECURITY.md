# 🔒 БЕЗОПАСНОСТЬ ПРОЕКТА

## ⚠️ ВАЖНО: Защита конфиденциальной информации

Этот проект работает с конфиденциальными документами и API ключами. Следуйте этим правилам безопасности:

## 🚫 ЧТО НЕ КОММИТИТЬ В GIT

### 1. API ключи и конфиденциальные настройки
- `config.py` с реальными API ключами
- `.env` файлы
- Файлы с паролями и токенами
- `secrets.py`, `credentials.py`

### 2. Документы для анализа
- Вся папка `documents/` (кроме `.gitkeep`)
- PDF, DOCX, PPTX, TXT файлы
- Контракты, договоры, отчеты
- Любые бизнес-документы

### 3. Результаты анализа
- Вся папка `reports/` (кроме `.gitkeep`)
- JSON и TXT отчеты
- Результаты анализа рисков
- Статистика по документам

### 4. Логи и отладочная информация
- Вся папка `logs/` (кроме `.gitkeep`)
- Файлы логов
- Отладочная информация
- Пользовательские данные

### 5. Промпты для LLM
- Вся папка `promts/` (кроме `.gitkeep`)
- Промпты могут содержать специфичные инструкции

## ✅ ЧТО МОЖНО КОММИТИТЬ

- Исходный код Python
- `requirements.txt`
- `README.md`
- `config.example.py` (шаблон без ключей)
- `.gitignore`
- Структуру папок (`.gitkeep` файлы)

## 🛡️ НАСТРОЙКА БЕЗОПАСНОСТИ

### 1. Создание безопасной конфигурации
```bash
# Скопируйте шаблон
cp config.example.py config.py

# Отредактируйте config.py с вашими ключами
# НИКОГДА не коммитьте config.py!
```

### 2. Использование переменных окружения (рекомендуется)
```bash
# PowerShell
$env:OPENAI_API_KEY = "ваш_ключ"
$env:OPENAI_API_BASE = "ваш_url"

# Или создайте .env файл (добавлен в .gitignore)
```

### 3. Проверка перед коммитом
```bash
# Проверьте, что не коммитите конфиденциальные файлы
git status

# Убедитесь, что config.py не в списке изменений
```

## 🔍 ПРОВЕРКА БЕЗОПАСНОСТИ

### Автоматическая проверка
```bash
# Проверьте, что .gitignore работает
git check-ignore config.py
git check-ignore documents/
git check-ignore reports/
git check-ignore logs/
```

### Ручная проверка
1. Убедитесь, что `config.py` не отслеживается git'ом
2. Проверьте, что папки с документами игнорируются
3. Убедитесь, что отчеты и логи не коммитятся

## 🚨 ЧТО ДЕЛАТЬ, ЕСЛИ КЛЮЧИ ПОПАЛИ В GIT

### 1. Немедленно отозвать ключи
- Смените API ключи в системе
- Отзовите старые ключи

### 2. Удалить из истории git
```bash
# Удалить файл из всех коммитов
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch config.py" \
  --prune-empty --tag-name-filter cat -- --all

# Принудительно обновить удаленный репозиторий
git push origin --force --all
```

### 3. Обновить .gitignore
- Убедитесь, что файл добавлен в .gitignore
- Проверьте, что работает правильно

## 📋 ЧЕКЛИСТ БЕЗОПАСНОСТИ

Перед каждым коммитом проверьте:

- [ ] `config.py` не изменен или не отслеживается
- [ ] `.env` файлы не добавлены
- [ ] Документы не попадают в коммит
- [ ] Отчеты и логи игнорируются
- [ ] Промпты не содержат конфиденциальной информации
- [ ] `.gitignore` обновлен при необходимости

## 🆘 ПОЛУЧЕНИЕ ПОМОЩИ

При возникновении проблем с безопасностью:

1. Немедленно отзовите скомпрометированные ключи
2. Проверьте историю коммитов
3. Обновите .gitignore
4. Обратитесь к команде безопасности

## 📚 ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ

- [GitHub Security Best Practices](https://docs.github.com/en/github/security)
- [Git Security](https://git-scm.com/book/en/v2/Git-Tools-Signing-Your-Work)
- [Python Security](https://python-security.readthedocs.io/)

---

**Помните: Безопасность - это ответственность каждого разработчика!**
